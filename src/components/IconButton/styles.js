//#region Imports
import { StyleSheet } from "react-native";
//#endregion

const useStyles = () => {
    return StyleSheet.create({
    icon: {
        padding: 10,
    }

    })
}

export default useStyles